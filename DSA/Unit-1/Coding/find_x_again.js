/*
Description :
Given a value of X, find the final value of X, if X is multiplied by 7 twice. 

Input:
Input will contain one number X
6

Output:
Output should be the number X after performing above multiply operations
294

Hint :
The value of X = 6, so after multiplying by 7 twice, it becomes 6 x 7 x 7, it becomes 294
*/

function findX(x) {
    // Write code here  
    console.log(x*7*7);
}

findX(6);
